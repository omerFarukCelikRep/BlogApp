global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'blogapp'
    static_configs:
      - targets: ['blogapp.api:5180']
    metrics_path: '/metrics'
    scheme: 'https'
    tls_config:
      insecure_skip_verify: true